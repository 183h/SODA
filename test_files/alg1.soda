STATES = INITIATOR, IDLE, DONE;
INIT = INITIATOR, IDLE;
TERM = DONE;

INITIATOR
  RANDOM
    begin
      SEND(x, NEIGHBORS);
      BECOME(DONE);
    end

IDLE
  RECEIVE(x)
    begin
      SEND(x, NEIGHBORS);
      BECOME(DONE);
    end